language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-xmc' ]; then cd .. && eval "mv $currentfolder generator-xmc" && cd generator-xmc; fi

